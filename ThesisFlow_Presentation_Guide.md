# ThesisFlow 产品全流程讲解手册（面试演示用）

> **文档定位**：供产品负责人向面试官/评审讲解 ThesisFlow 全流程功能的演示手册。按「模块一 → 模块二 → 模块三 → 模块四」的产品视角编排，每模块给出演示目标、操作动线、讲解要点与预期效果。产品内不再提供演示引导面板，讲解过程由演讲者自行控制节奏。
> 产品设计细节见 `ThesisFlow_PRD_Detailed.md`，工程与启动说明见 `README.md`，30 秒产品卡片见 `Product_Overview.md`。

## 0. 演示前准备

| 项 | 内容 |
|----|------|
| 演示人设 | 小林 · AI 领域博士生（研究方向：上下文工程与长上下文 RAG、Agent 记忆体系）· APA 引用 · 中文写作 |
| 演示课题 | 《AI 长上下文管理的工程化措施综述与展望》 |
| 种子数据 | 24 篇真实 arXiv 论文（20 篇主题域 + 4 篇主题外低相关样本；PDF/Word/Markdown 三格式）+ 小林研究画像 + 10 条高置信记忆 + 预生成脉络图谱 |
| 建议时长 | 15 分钟（模块一 3 分钟 · 模块二 3 分钟 · 模块三 3 分钟 · 模块四 5 分钟 · 收尾 1 分钟） |
| 核心卖点 | ① 全链路工作台闭环（入库→筛选→精读→写作信息不断流）② 长效记忆沉淀（越用越懂你）③ 提案式写作与引用溯源（采纳才落笔 + APA 引用点击定位原文） |

**启动与种子构建**（详见 README 快速启动）：

```bash
cd backend && cp .env.example .env   # 填 DeepSeek 与百炼 API Key
python3 serve.py                     # 后端 :8000
cd ../frontend && npm install && npm run dev   # 前端 :3000

# 首次构建种子数据（约 20-35 分钟，仅需一次）
cd backend
uv run python scripts/download_demo_papers.py
uv run python scripts/seed_demo.py
```

**排练前检查**：

- [ ] 打开 `http://localhost:3000`，主页正常展示知识挖掘流与项目进展摘要；
- [ ] 项目《AI 长上下文管理的工程化措施综述与展望》下 24 篇文献全部就绪，其中 3 篇低相关样本总分低于 2.5（折叠生效）；
- [ ] 左下角「个人设置」研究画像为「博士生 · 人工智能」；个人记忆库有 10 条记忆；
- [ ] 预热：预先打开一次写作台与阅读器页面（首帧加载、PDF 渲染就绪），避免上台等待。

**随时复位**：讲解中若数据被弄乱（乱批注/乱草稿），左下角「个人设置」弹窗底部「重置演示数据」一键秒级恢复种子状态（或 `POST /api/admin/demo-reset`），不重跑 AI 任务。

---

## 模块一 · 领域知识库 —— 长效记忆唤醒（约 3 分钟）

### 演示目标

展示系统「记得我」：研究画像与长期记忆挂载进每一次 AI 会话，开题即带学术默契；同时展示研究图景与知识挖掘能力。

### 操作动线

1. 进入主页（全局控制台）：展示知识挖掘流（文献库知识挖掘动态）与项目进展 AI 摘要；
2. 左下角「个人设置」打开弹窗：展示研究画像（AI 博士生 · 上下文工程与长上下文 RAG · APA · 中文）；
3. 左侧「领域知识库 → 个人记忆库」：展示 10 条高置信度记忆（如 Lost-in-the-Middle 与注意力衰减机制关注、KV Cache 内存开销视角、因果推断与机制分析偏好等）；
4. （可选）「领域知识库 → 研究图景」：展示思维导图，点击节点看进展解读与研究缺口。

### 讲解要点

- 三层 System Prompt 动态拼接：`[Base_Persona]（恒定人设）+ [User_Context]（身份/领域/引用规范）+ [Domain_Memory]（置信度 top-10 长期记忆）`，随每次请求自动装配——AI 不是每次从零开始；
- 记忆有完整生命周期：隐式提取 + 显式录入双通道、置信度升降、冲突裁决卡、健康报告；
- 后续在写作环节可以呼应：这些记忆正是从日常商讨中隐式沉淀下来的。

### 预期效果

- 记忆库列表 10 条、全部带高置信度徽标；研究画像与演示人设一致；
- 听众理解：「这是一个带着我的研究背景参与每次对话的系统」。

---

## 模块二 · 文献筛选矩阵 —— 20+ 篇文献高效筛选（约 3 分钟）

### 演示目标

展示批量入库 → 四维打分 → 矩阵排序 → 低分折叠 → 脉络图谱的完整筛选闭环。

### 操作动线

1. 左侧项目空间点开《AI 长上下文管理的工程化措施综述与展望》→ 文献工作台；
2. 口播多格式批量入库（24 篇 = 22 PDF + 1 Word + 1 Markdown，全部就绪）；
3. 展示四维打分矩阵（质量/相关性/方法论/创新性）与每项评分理由直显；展开/收起 <2.5 分低分文献（4 篇主题外低相关样本，指出其「相关性 1 分」的评分理由）；
4. 演示人工校正：分数格悬停 ✎ → 改分 + 填写校正理由 → 保存后「人工」徽标即时生效，讲解该校正将作为 few-shot 样例校准后续打分（越校正越准）；
5. 拖动「相关性」权重滑块观察实时重排，锁定 Top 10 核心文献；
6. 切换「文献脉络图谱」视图：主题聚类分支 + 关系边（extends/supports/contrasts 等）+ 脉络叙述。

### 讲解要点

- 入库即结构化：文献被切成带完整元数据的片段，这是后续所有环节共用的最小单位；
- 打分与检索联动：高分文献摘要获得更高检索权重，写作时自然被优先引用；
- 成本-质量分级：打分走轻量模型、图谱叙事走强模型，按任务敏感度分槽调用。

### 预期效果

- 权重滑块拖动后排序变化 <1s；图谱有 ≥2 个聚类分支并可展开分支叙述；
- 听众理解：「横向对比几十篇文献从手工劳动变成秒级结构化矩阵」。

---

## 模块三 · 沉浸式精读 —— 批注升权与问答溯源（约 3 分钟）

### 演示目标

展示三栏精读体验与「精读即投喂」机制：划线批注实时升权，伴读问答可点击溯源。

### 操作动线

1. 在文献工作台点击《Lost in the Middle》标题 → 进入阅读器（左：结构化预读卡；中：PDF 原文；右：批注沉淀 + 伴读问答）；
2. 左栏展示一页纸预读卡（核心问题/方法/结论/贡献/局限五分区）；
3. 在 PDF 原文划选关键结论 → 打标「重点论据」→ 讲解该片段实时升为最高检索权重（Top Tier ×2.0）；
4. 右栏伴读问答提问「该文献的注意力衰减实验结论是什么？」→ 回答携带 `[c1]` 锚点徽标 → 点击跳转对应页并高亮定位原文；
5. （可选）对 Word/Markdown 文献演示结构化文档视图划选批注，同样支持溯源闪烁定位。

### 讲解要点

- 精读即投喂：在原文上划一条线、打一个标，该片段立即成为写作时系统优先「注意」的内容；
- 伴读问答基于本文档证据（top4 chunks）回答，每个答案都有原文锚点可点击回跳——回答可核查；
- 批注自动绑定原文片段，笔记不再「躺在 PDF 里用不上」。

### 预期效果

- 打标后右栏批注区实时新增条目；问答回答含可点击 `[cN]` 徽标，点击后 PDF 跳页并闪烁高亮；
- 听众理解：「精读产生的笔记直接变成了后续写作的高权重素材」。

---

## 模块四 · 多模式写作工作台 —— 提案式写作与引用溯源（约 5 分钟）

### 演示目标

展示起草 → 写作 → 审查三模式闭环，重点讲提案卡机制（AI 不直接改稿）与 APA 引用溯源（点击定位原文）。

### 操作动线（起草模式，约 1.5 分钟）

1. 进入写作工作台「起草」模式，与 AI 商讨 2-3 轮论文大纲结构（对话持久化，刷新不丢）；
2. 讲解：对话每满 4 轮，系统后台用轻量模型从讨论中提取新研究偏好（如「分析长上下文需引入 KV Cache 内存开销视角」），语义比对后增量合并入记忆池——呼应模块一；
3. 点「同步到大纲」一键生成 Markdown 结构化大纲（可编辑/预览）。

### 操作动线（写作模式，约 2.5 分钟）

1. 切「写作」模式，输入指令「/续写 结合 Top 10 文献与我的批注，撰写关于注意力机制在超长上下文下的局限性」；
2. AI 输出**提案卡**（整卡审核，含改动说明）：展示 APA 文内引用（作者年份，如 Liu et al., 2023）与三色引用状态徽标——蓝 = 引用正常、黄 = 弱支持、红 = 无效引用（系统自动校验标注）；
3. 点击角标 → 右侧打开原文对应位置高亮（引用溯源闭环）；
4. 点「采纳」→ 段落与引用节点原子化写入编辑器；「拒绝」则继续对话讨论。口播：AI 永不直接改稿，决策权在研究者手中。

### 操作动线（审查模式，约 1 分钟）

1. 切「审查」模式 → 「审查全文」：五维建议卡（论证充分性/逻辑连贯性/结构完整性/学术规范性/方法严谨性，17 检查点），点击锚点定位对应段落；
2. 勾选 1-2 张问题卡 → 「AI 一键修改所选问题」→ 逐条 Diff 卡审核（✓ 校验通过 / ⚠ 需人工复核）；
3. 采纳 → 正文替换 + 版本快照留存；建议卡置灰显示「✓ 已修复」防重复修改；可演示版本历史回退。

### 讲解要点

- 模式感知装配：起草喂骨干摘要促发散、写作喂当前段落 + 高权重片段（含模块三的批注）收聚焦点、审查只评文本本身——同一文献库在不同场景被裁成不同上下文；
- 提案卡契约：追加/修改/删除三模式，锚点精确插入、多处出现明确报错不静默替换；
- 每次采纳自动留快照，全程可回退——修改始终可控、可回溯。

### 预期效果

- 提案卡出现可点击引用角标 + 蓝色徽标；采纳后正文一次写入、光标位置正确；
- 审查采纳后卡片置灰、快照 +1；
- 听众理解：「从入口到出口，AI 只给提案，采纳权与回退权始终在人手中」。

---

## 收尾 · 全链路记忆回环（约 1 分钟）

- 回到「个人记忆库」：讲解刚才写作商讨中隐式沉淀的新偏好已出现在记忆池（或触发了冲突裁决卡，两个结果都是亮点）；
- 点题：四个模块信息不断流（入库即结构化 → 打分联动检索权重 → 精读批注即投喂 → 写作按模式装配上下文），记忆让系统随使用持续进化——「越用越懂你」。

## 附录：种子数据与重置

- 种子内容：小林画像 + 10 条高置信记忆 + 演示项目 + 24 篇文献（20 篇主题域 + 4 篇低相关样本）+ 脉络图谱 + 快照（`backend/data/demo_fixture.json`）；
- 重置方式一：左下角「个人设置」弹窗 → 底部「重置演示数据」按钮；
- 重置方式二：`POST /api/admin/demo-reset`（秒级恢复种子状态，不重跑 LLM）；
- 种子重建：若快照缺失/损坏，重跑 `uv run python scripts/seed_demo.py`（约 20-35 分钟）。
