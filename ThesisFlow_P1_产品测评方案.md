# ThesisFlow 产品测评方案

> 文档状态：V1.0，已审核通过
> 测评对象：已完成 P0「主流程决策与可信度升级」的 ThesisFlow Demo
> 测评主题：AI 长上下文工程设计
> 结果定位：小样本离线测评与本人自用验证，不外推为独立用户验证或全量推广效果

## 1. 审核摘要

本方案用于验证 ThesisFlow 在「文献筛选 → 文献精读 → 阅读成果复用 → 人机协作写作」主流程中的准确性、可核查性、操作可靠性和实际任务成本。

本轮不以证明「产品已提效」为前提，而是建立可复现的 P0 版本基线，找到首次失败环节，用固定任务比较修改前后的变化，并在方案冻结后使用保留集复测。

### 1.1 已确认的五项关键决策

| # | 决策项 | 已确认口径 | 原因 |
|---|---|---|---|
| 1 | 核心任务数量 | 30 个：筛选 10、精读 10、协作写作 10 | 解决原计划「约 30 个」与「四类各 10 个」的数量冲突；批注转化纳入协作写作 |
| 2 | 记忆与反馈测评 | 设 8 项专项回归检查，不计入 30 个核心任务 | 其中多为范围、冲突和操作安全检查，与三个主场景的效果指标分开更清楚 |
| 3 | 样本范围 | 使用现有 24 篇演示文献：20 篇主题相关文献 + 4 篇低相关对照文献 | 无需重新收集，同时覆盖相关、边界、低相关和信息不足情形 |
| 4 | 核心对照 | P0 基线 vs. 修改后候选版本；人工流程和通用 AI 仅做少量代表任务 | 优先回答「迭代是否带来改善」，同时控制测评成本 |
| 5 | 进入保留集的条件 | 关键安全门槛全部通过，且开发集主指标有改善或持平、无明显回归 | 避免为了追求单一均分而牺牲引用可信度或用户内容安全 |

## 2. 测评目标与非目标

### 2.1 要回答的问题

1. 文献筛选：系统能否优先找到值得精读的文献，并给出可由原文和项目标准支撑的理由？
2. 文献精读：系统能否帮助完成方法、结果、限制和原文定位任务，并在证据不足时停止推断？
3. 协作写作：系统能否按批注标签的原始用途复用材料，生成主张—证据对应、修改范围可控、可回退的内容？
4. 端到端价值：相比本人原流程，使用产品时的查找、核验、纠正和返工成本如何变化？
5. 可迭代性：问题能否定位到具体的首次失败环节，并在修改后通过同任务和回归集复现变化？

### 2.2 本轮非目标

- 不验证多人协作、全学科泛化或规模化用户增长。
- 不将任务跑通视为准确、提效或用户价值的证明。
- 不将用户的采纳、拒绝或表达偏好直接当作内容正确性标签。
- 不因单次模型输出推断「越用越准」；该假设只能通过后续重复任务观察。
- 不在本轮开展独立用户研究、大规模 A/B 测试或统计显著性宣称。

## 3. 测评范围与对象

### 3.1 受测产品链路

| 环节 | P0 已有能力 | 本轮主要风险 |
|---|---|---|
| 入库与解析 | PDF / Word / Markdown 入库，切片绑定页码、章节和 chunk | 解析不完整、页码或 chunk 错配，导致下游全部失败 |
| 筛选 | 四维评分与 AI 精读建议分离；信息不足允许为空；折叠可关闭 | 重要文献漏掉、理由空泛、把元数据缺失当低质量、错误折叠 |
| 精读 | 预读卡、阅读重点、伴读问答、原文定位、四类批注 | 回答超出证据、关键限制遗漏、定位失败却未暴露 |
| 协作写作 | 批注素材区、追加/替换/删除提案、三层引用校验、过期提示、快照回退 | 标签语义被扭曲、主张无证据、越界修改、静默丢文、错误回退 |
| 记忆与反馈 | 当前指令优先；记忆可查看/编辑/删除；采纳/拒绝仅作行为记录 | 历史偏好覆盖当前指令、跨项目泄漏、错误归因导致文献降权 |

### 3.2 文献样本池

现有演示数据包含 24 篇可用文献，与 P1 的「约 20 篇」一致，并额外提供 4 篇低相关对照。正式执行前建立 `document_manifest`，固定以下字段：

| 字段 | 说明 |
|---|---|
| `doc_id` | 测评稳定编号 D01–D24，不使用会随重置变化的数据库 ID |
| `title` / `arxiv_id` | 题名和可核对的文献标识 |
| `topic_group` | 长上下文核心、RAG/GraphRAG、评估与校验、Agent 记忆与效率、低相关对照 |
| `file_type` | PDF / Word / Markdown |
| `expected_relevance` | 高 / 中 / 低 / 信息不足 |
| `priority_label` | 推荐精读 / 暂不优先 / 信息不足 |
| `gold_basis` | 与研究问题、纳入标准和原文对应的人工判断依据 |
| `split` | dev / holdout |
| `parse_status` | 就绪 / 部分缺失 / 不可用，及发生原因 |

样本分层如下：

- 长上下文核心：Lost in the Middle、LongRoPE、RULER、LongBench、StreamingLLM、Position Interpolation、输入长度与推理表现。
- RAG 与 GraphRAG：RAG 原始论文、RAG 综述、GraphRAG、Self-RAG、CRAG、LightRAG。
- 检索评估与生成校验：RAGAS、SelfCheckGPT、FActScore、幻觉综述。
- Agent 记忆与效率：Agent 记忆机制综述、LLM 高效推理综述、L-Eval。
- 低相关对照：基因组解读、磁热效应、湍流预混火焰、矮星系中性氢。

### 3.3 开发集与保留集

- 30 个核心任务按场景分层为 dev 21 个、holdout 9 个；每个场景均为 7:3。
- 优先进行文献级隔离；如因跨文献综合必须复用文献，则至少保证同一主张—证据单元不跨集合。
- 同一问题的改写、同一原文事实的换问法、同一锦标的近义表达不得分散到两个集合。
- holdout 的标准答案、引文位置和评分明细在方案冻结前不用于 Prompt、few-shot 示例或代码调试。
- 任务分配固定后生成哈希或 Git 提交号，后续任何改动单独记录。

## 4. 测评设计

### 4.1 整体结构

| 层级 | 要验证的内容 | 产出 |
|---|---|---|
| L0 功能可靠性 | 入库、定位、批注、提案、引用、导出、回退、异常恢复能否可重复完成 | 必过检查表与失败日志 |
| L1 AI 能力 | 推荐、理由、预读、问答、批注复用、主张—证据对应是否正确 | 30 个核心任务的逐题评分 |
| L2 场景价值 | 在质量不下降前提下，是否减少时间、遗漏、核验和返工 | 本人自用记录和对照结果 |

### 4.2 对照条件

| 条件 | 用途 | 覆盖范围 |
|---|---|---|
| A. 人工原流程 | 建立任务时间、遗漏和核验成本参照 | 每场景 2–3 个代表任务 |
| B. 通用 AI | 观察产品工作流与一般对话式工具的差别 | 每场景 2–3 个代表任务，不作全量对照 |
| C. ThesisFlow P0 基线 | 固定本轮迭代起点 | dev + holdout 全量，holdout 仅在冻结后跑一次 |
| D. ThesisFlow 候选版本 | 验证具体修改是否改善问题 | dev 可重复；通过门槛后进入 holdout |

对照时固定文献版本、研究问题、筛选标准、模型及配置、任务指令、操作起点和计时规则。当被测改动本身是 Prompt、检索或上下文装配时，只放开该自变量，其他条件保持不变。

### 4.3 Prompt 全链路记录协议

#### 4.3.1 记录原则

测评不只保存用户在界面输入的问题，而是保存每次模型调用的完整可见输入链。同一次调用必须能区分：

1. **System Prompt 模板**：未注入用户、项目或记忆内容的源模板。
2. **实际渲染的 System Prompt**：真正发送给模型的系统消息，包括用户背景、活跃记忆、引用规范和标签语义等已注入内容。
3. **用户原始指令**：用户在界面中实际输入的文字，不与应用生成的 Prompt 混在一起。
4. **User Prompt 模板**：应用层为当前环节定义的任务说明、输出格式和约束。
5. **实际渲染的 User Prompt**：将研究问题、文献内容、RAG 证据、批注、正文选区等注入后，真正发送给模型的用户消息。
6. **完整 messages 序列**：按实际顺序保存 system / user / assistant 历史消息的 role 与 content，不只保存最后一条问题。
7. **上下文装配清单**：保存本次调用使用的记忆 ID、文献 ID、chunk key、批注 ID、大纲版本、正文版本、历史轮次和裁剪/截断决策。
8. **调用配置与结果**：模型、提供方、temperature、max tokens、JSON 模式、耗时、token 用量、缓存状态、原始输出和解析后输出。

不要请求或记录模型的隐藏思维过程；测评只记录应用和 API 可见的消息、上下文、输出与运行元数据。

#### 4.3.2 需覆盖的 Prompt 环节

| 环节 | 必记 Prompt/消息 | 必记动态上下文 | 是否属于核心测评 |
|---|---|---|---|
| 文献解析与标题修正 | 元数据或标题修正 Prompt；若无 System Prompt 记为 `none` | 文件名、首页/正文片段 | 是，影响下游时 |
| 四维评分与精读建议 | 评分/推荐 User Prompt；System Prompt 当前未使用则记为 `none` | 研究问题、筛选标准、学科参考、人工校准样例、摘要和结论 | 是，S01–S10 |
| 预读卡与阅读重点 | 预读 Prompt；无 System Prompt 则明记 | 题名、摘要、正文开头及截断长度 | 是，R01–R10 |
| 概念解释 | 概念解释 Prompt | 术语、用户选中上下文 | 按任务是否调用 |
| 精读伴读问答 | 实际 System Prompt、应用生成的问答指令、用户原问题、对话历史 | 召回的 chunk、页码/章节、top-k、截断和缓存命中 | 是，R01–R10 |
| 起草商讨 | 实际 System Prompt、起草讨论 Prompt、用户当轮指令与历史 | 研究问题、文献骨干摘要、记忆 | 是，若 W 任务调用 |
| 大纲生成 | 大纲 User Prompt，无 System Prompt 则明记 | 起草商讨记录、研究问题、对话截断 | 是，若 W 任务调用 |
| 续写/追加/替换/删除 | 实际 System Prompt、写作任务 Prompt、用户原始指令、历史 | 大纲路径、正文窗口、选区、选中批注、检索证据、起草上下文 | 是，W01–W10 |
| 快捷润色/重写/逻辑检查 | 实际 System Prompt 与快捷操作 Prompt | 原文选区、补充指令、原引用标记 | 是，若 W 任务调用 |
| 审查、修复与复核 | 审查 Prompt、修复 Prompt、修复结果复核 Prompt 分别记录 | 待审文本、问题锦标、严重度、原文锚点、引用标记 | 是，W08–W10 |
| 引用 NLI 校验 | NLI 判定 Prompt，无 System Prompt 则明记 | 主张、证据 chunk、向量快筛分数、高风险规则 | 是，W01–W10 |
| 记忆提取 | 记忆候选提取 Prompt | 被选入的对话轮次、截断范围、项目来源 | 是，M01–M08 |
| 研究图景、文献图谱、首页摘要 | 各自 Prompt 与模型调用 | 被纳入的文献/记忆、摘要长度和增量范围 | 非核心；当它们影响受测任务时必记 |

不存在 System Prompt 的调用必须显式记录 `system_prompt_present=false`，不能把缺失记录解释为「当时没有 System Prompt」。

#### 4.3.3 每次模型调用的最小记录结构

```json
{
  "prompt_call_id": "PC-20260913-0001",
  "run_id": "P1-BASELINE-001",
  "task_id": "W05",
  "stage": "writing_proposal",
  "parent_call_id": null,
  "prompt_template_id": "writing.proposal",
  "prompt_template_version": "1.0.0",
  "system_prompt_present": true,
  "system_prompt_template": "...",
  "system_prompt_rendered": "...",
  "raw_user_instruction": "...",
  "user_prompt_template": "...",
  "user_prompt_rendered": "...",
  "messages": [{"role": "system", "content": "..."}],
  "context_manifest": {
    "project_id": 1,
    "document_ids": [1, 2],
    "chunk_keys": ["1:3", "2:7"],
    "annotation_ids": [12],
    "memory_ids": [3, 6],
    "draft_version": "sha256:...",
    "truncation": [{"field": "editor_text", "original_chars": 12000, "kept_chars": 8000}]
  },
  "model": {"provider": "...", "name": "...", "temperature": 0.4, "json_mode": true},
  "cache": {"hit": false, "key": "..."},
  "prompt_sha256": "...",
  "raw_output": "...",
  "parsed_output": {},
  "usage": {"prompt_tokens": 0, "completion_tokens": 0, "latency_ms": 0},
  "error": null
}
```

`parent_call_id` 用于连接同一任务的多步调用，例如「写作生成 → 引用 NLI 校验」或「审查 → 修复 → 复核」。

#### 4.3.4 版本、安全与导出规则

- 模板使用稳定 ID 和版本号，每次文字、示例、输出 schema 或约束变更都必须升版。
- 对模板和实际渲染 messages 分别计算 SHA-256，用于检测静默变更；哈希不替代可读的原文记录。
- 原始 Prompt 日志仅保存在本地受控测评目录；API Key、token、连接密钥和其他秘密必须在写盘前脱敏。
- 研究文献片段和个人记忆可保留在内部原始日志中，但对外展示的测评报告只放文献/chunk/记忆编号和必要摘要。
- 缓存命中时也要生成调用记录，标明「未向模型发起新请求」以及返回的原始调用 ID。
- 如模型供应商在服务端添加不可见的指令，记录为 `provider_prompt=unavailable`，不对不可见内容做推测。

#### 4.3.5 基线开始前的工程门槛

当前系统的 LLM 可观测记录主要包含模型、token、延迟和部分前缀哈希，不足以还原实际 System Prompt、User Prompt 和上下文装配。因此，开始 P0 基线运行前必须先完成 Prompt 记录与导出能力；无法完整记录的运行只能用于调试，不计入正式基线。

## 5. 30 个核心任务

### 5.1 文献筛选（S01–S10）

筛选任务以「用户决策」为任务单元，以「单篇文献的标签或理由」为可评分输出单元。因此 10 个任务可覆盖 24 篇文献，指标分母以实际被判断的文献数为准。

| ID | 任务 | 覆盖风险 | 主要可评分输出 |
|---|---|---|---|
| S01 | 从长上下文核心文献中选出首轮必读集 | 重要文献遗漏 | 精读标签、排序、理由 |
| S02 | 评估上下文窗口扩展方法的阅读优先级 | 相关性与方法价值混淆 | 分类与依据 |
| S03 | 评估 RAG / GraphRAG 文献对研究问题的直接性 | 宽泛主题相关被误当核心证据 | 分类与依据 |
| S04 | 评估可靠性与事实性评估文献 | 把「校验方法」与「长上下文管理」直接等同 | 分类与边界说明 |
| S05 | 评估 Agent 记忆与高效推理文献 | 边界相关文献误判 | 分类与使用范围 |
| S06 | 在元数据不完整时给出精读建议 | 缺失信息补猜 | 「信息不足」标记与缺失项 |
| S07 | 识别 4 篇跨学科低相关对照文献 | 假阳性与错误高分 | 「暂不优先」及理由 |
| S08 | 检查自动折叠后的可见文献 | 推荐精读/信息不足文献被隐藏 | 折叠合规数、错误隐藏数 |
| S09 | 逐条核对推荐理由 | 理由空泛、超出原文或与项目无关 | 理由三维评分 |
| S10 | 在给定阅读时间预算下形成最终阅读队列 | 评分与建议冲突时的用户决策 | Top-N 召回、顺序偏差、人工纠正 |

### 5.2 文献精读（R01–R10）

| ID | 任务 | 覆盖类型 | 最低预期结果 |
|---|---|---|---|
| R01 | 概括 Lost in the Middle 的核心问题与主要发现 | 核心问题/结果 | 回答准确，能定位到相应原文 |
| R02 | 说明 LongRoPE 的方法构成和评测范围 | 方法/边界 | 区分论文声称、实验条件与可推广范围 |
| R03 | 比较 RULER 与 LongBench 测量的能力 | 概念比较 | 指出共同点、差异和不可直接类比之处 |
| R04 | 解释 StreamingLLM / Attention Sinks 的机制 | 方法理解 | 不把流式处理效果等同于任意长度的全局理解 |
| R05 | 说明 Position Interpolation 的关键做法和局限 | 方法/限制 | 至少命中一项经人工标注的关键限制 |
| R06 | 说明 GraphRAG 「local-to-global」的处理链路 | 流程理解 | 流程顺序正确，不混入其他 GraphRAG 实现细节 |
| R07 | 区分 Self-RAG 与 CRAG 的问题定义和修正机制 | 跨文献比较 | 每个主张可追溯至对应文献 |
| R08 | 识别 RAGAS / FActScore 各自不能直接证明的内容 | 限制/过度推断 | 能明确拒绝至少一个超出证据的结论 |
| R09 | 根据一个结果主张跳转并核对原文 | 定位 | 文献、chunk、页码/章节均正确；失败时明示 |
| R10 | 对文献中未提供的细节提问 | 信息不足 | 不编造答案，明确指出当前证据不足 |

### 5.3 批注复用与协作写作（W01–W10）

| ID | 任务 | 覆盖类型 | 最低预期结果 |
|---|---|---|---|
| W01 | 将「重点论据」批注转为带引用的支持段落 | 批注转化 | 主张不超出批注与原文，引用支持主张 |
| W02 | 将「借鉴方法」批注写成方法参考 | 标签语义 | 不把借鉴的方法写成已证实结论 |
| W03 | 将「存疑」批注用于限制或争议段落 | 反证与限制 | 不将存疑材料当作支持证据 |
| W04 | 将「背景」批注转为定义或背景说明 | 背景边界 | 不用背景性文字替代方法效果证据 |
| W05 | 综合 RULER、LongBench 与 L-Eval 形成评测方法对比段 | 跨文献综合 | 来源归属正确，明示不可比条件 |
| W06 | 综合 RAG、GraphRAG、Self-RAG 和 CRAG 形成方法谱系段 | 多来源组织 | 不把后续方法特性倒置归给早期论文 |
| W07 | 综合正向证据与局限，写出有边界的结论 | 冲突材料 | 同时保留支持与限制，不单边丢失证据 |
| W08 | 仅替换指定段落，其他正文保持不变 | 定点修改 | 修改范围符合指令，提案采纳后无残留字符或多余换行 |
| W09 | 在正文变化后处理过期提案与定位冲突 | 提案生命周期 | 旧提案不静默写入，可重试，用户内容保留 |
| W10 | 检查引用后拒绝一次提案，再采纳修改并回退 | 引用/采纳/回退 | 三层校验状态正确；拒绝不改正文；回退结果与快照一致 |

## 6. 标准答案与评分规则

### 6.1 标准答案制作顺序

1. 固定项目研究问题、筛选标准与文献版本。
2. 不启动 ThesisFlow，直接阅读原文并写出每张任务卡的预期结果。
3. 为每个事实或主张绑定原文依据：文献、页码/章节、原文摘要及允许的语义范围。
4. 区分「必须命中」、「可接受表达」、「明确不可接受」和「存在争议」。
5. 在首次系统运行前锁定标签和评分规则。如发现标准错误，单独记录更正日志，不得只针对某个版本追溯修改标准。

### 6.2 文献筛选评分

#### 分类指标

- 重要文献召回率 = 正确标为「推荐精读」的重要文献数 / 人工标注的重要文献总数。
- 推荐宏平均 F1 = 对「推荐精读 / 暂不优先 / 信息不足」三类分别计算 F1 后取平均，避免多数类掩盖少数类。
- Top-N 命中率 = 阅读预算为 N 时，系统前 N 篇中人工必读文献的占比。
- 错误折叠数 = 被系统折叠的「推荐精读」或「信息不足」文献数。

#### 理由可靠性（0–6 分）

| 维度 | 0 分 | 1 分 | 2 分 |
|---|---|---|---|
| 任务对齐 | 未回应研究问题或筛选标准 | 主题相关但未解释决策 | 明确说明文献如何影响阅读优先级 |
| 证据一致 | 理由与原文冲突或凭空生成 | 大体有依据但范围过宽 | 关键理由可由原文支撑，范围恰当 |
| 不确定性 | 补猜缺失信息 | 表达含糊但未造成实质错误 | 明确标出缺失项、争议或判断边界 |

### 6.3 文献精读评分（0–10 分/题）

| 维度 | 分值 | 判定要点 |
|---|---:|---|
| 结论正确性 | 0–2 | 2 = 主结论与标准一致；1 = 部分正确或有非关键偏差；0 = 核心错误 |
| 关键点覆盖 | 0–2 | 按任务卡预先列出的必须命中项计分 |
| 范围与限制 | 0–2 | 是否保留适用条件、局限和不可推断项 |
| 来源正确 | 0–2 | 引用文献与主张归属正确，不张冠李戴 |
| 原文定位 | 0–2 | 2 = 可直达正确原文；1 = 文献正确但位置偏差；0 = 无法定位或定位错误 |

另记录「关键限制遗漏数」和「信息不足时编造回答数」，两者不被总分抵消。

### 6.4 协作写作评分（0–12 分/题）

| 维度 | 分值 | 判定要点 |
|---|---:|---|
| 任务完成 | 0–2 | 是否完成指令要求的段落或修改 |
| 批注用途保持 | 0–2 | 论据/方法/存疑/背景四类语义是否被正确使用 |
| 主张—证据对应 | 0–2 | 每个可验证主张是否有支持证据，证据强度是否匹配 |
| 综合与边界 | 0–2 | 能否区分来源、处理冲突并保留局限 |
| 提案范围 | 0–2 | 是否只修改指定范围，无静默改写和内容丢失 |
| 引用可核查性 | 0–2 | 来源存在、定位正确、证据支持三层状态分开且正确 |

额外记录采纳前人工修改字符数、修改轮数、引用核验次数、反工时间、采纳/拒绝/过期/定位失败/回退结果。

### 6.5 单题通过口径

- 筛选题：每张任务卡在运行前写明最低命中数或错误上限；发生重要文献错误折叠、缺失信息补猜或关键理由与原文冲突时，该题直接不通过。
- 精读题：总分至少 8/10，且「结论正确性」「来源正确」均不得为 0；编造信息或把错误原文作为依据时直接不通过。
- 协作写作题：总分至少 9/12，且「主张—证据对应」「提案范围」均不得为 0；静默丢失用户内容、引用造假被判定为支持或跨项目记忆泄漏时直接不通过。
- 与阈值同时报告原始分数和失败类型，不将「刚好过线」与「满分」视为等价。

### 6.6 场景价值指标

| 指标 | 起点 | 终点 | 报告方式 |
|---|---|---|---|
| 任务总耗时 | 读完任务卡 | 产出达到预期或确认无法完成 | 每题分钟数、中位数、范围 |
| 查找素材时间 | 开始搜索/浏览 | 确认要使用的原文或批注 | 单独报告，不与生成等待混合 |
| 核验时间 | 开始核对 AI 输出 | 完成所有必要的原文检查 | 分钟数和原文跳转次数 |
| 人工纠正 | 首次 AI 输出完成 | 结果达标 | 纠正次数、字符数、纠正类型 |
| 返工成本 | 首次宣告任务完成 | 发现错误后再次达标 | 返工次数与时间 |
| 盲从 | 首次 AI 错误建议 | 用户做出决策 | 未核查即采纳且未自行纠正的错误数 |

时间指标只在质量持平或提升时解读。如果耗时下降但严重错误增加，不记为有效提效。

## 7. 记忆与反馈专项回归

下列 8 项与 30 个核心任务分开计数，每项记录通过/失败、实际输入、实际输出和首次失败环节。

| ID | 条件 | 预期结果 |
|---|---|---|
| M01 | 关闭记忆后执行一个写作任务 | 输出不隐式使用历史偏好 |
| M02 | 正确个人写作偏好与当前任务一致 | 在不改变事实内容的前提下正确应用 |
| M03 | 当前指令与历史偏好冲突 | 当前明确指令优先 |
| M04 | 在项目 B 中执行任务，而相关背景只属于项目 A | 项目 A 的事实不进入项目 B |
| M05 | 研究问题变更 | 对受影响的建议或记忆给出需重新核对的提示 |
| M06 | 编辑一条记忆后重复任务 | 新内容生效，旧内容不再被使用 |
| M07 | 删除一条记忆后重复任务 | 已删除内容不再被召回或影响输出 |
| M08 | 拒绝一次仅因表达风格不合适的提案 | 行为被记录，但对应文献不因此自动降权 |

M03、M04、M07、M08 属于安全性必过项，不用其他项的高分抵消。真实的「减少重复交代」只在后续自然发生的重复任务中记录，不用一次模拟会话作结论。

## 8. 首次失败环节与问题分级

### 8.1 首次失败环节

每个失败只先记录一个「首次失败环节」，同时可附加下游影响标签。

| 编码 | 环节 | 典型表现 |
|---|---|---|
| F1 | 文件与解析 | 文件未就绪、正文缺失、标题/页码错误 |
| F2 | 切片与元数据 | chunk 截断语义、章节标签错误、位置信息丢失 |
| F3 | 评分与推荐 | 相关性错判、信息不足误判、折叠错误 |
| F4 | 检索与重排 | 正确证据未被召回、存疑材料排在支持证据之前 |
| F5 | 上下文装配 | 标签丢失、错项目记忆混入、当前指令未优先 |
| F6 | 模型判断与生成 | 在证据正确的情况下仍误读、夸大或编造 |
| F7 | 引用校验 | 来源存在/定位/证据支持状态错判 |
| F8 | 交互与写入 | 锚点错位、越界替换、失败后丢内容、回退不一致 |
| F9 | 人工标准/操作 | 标准答案有误、任务卡含糊、计时或操作不一致 |

### 8.2 问题严重度

| 级别 | 定义 | 例子 |
|---|---|---|
| P0 阻断/安全 | 结果不可信或用户内容受损，且无明确恢复手段 | 编造引用被标为支持、静默覆盖正文、错项目记忆泄漏 |
| P1 主要 | 核心任务失败，但可被用户发现或恢复 | 必读文献遗漏、关键限制丢失、定位失败明示 |
| P2 次要 | 不影响核心结论，但显著增加纠正或理解成本 | 理由偏空泛、需多一次跳转才能定位 |
| P3 体验 | 轻微表达或展示问题 | 文案不统一、非关键格式偏差 |

## 9. 执行流程

### 9.1 基于独立工作台的人机协作方式

人工协作通过独立运行的「测评优化人工协作工作台」完成，不在 ThesisFlow 内增加测评功能。工作台只用于展示现状、引导人工审核和保存反馈，不修改 Prompt、RAG、模型或 ThesisFlow 数据。

工作台提供两个页面：

1. **任务现状：**按文献筛选、文献阅读、辅助写作展示每项能力；点击后可查看对应的 ThesisFlow 模块、当前 System/User Prompt、检索规则、评价指标和已有结果。没有实际结果时明确显示「尚无测试结果」。
2. **人工审核：**自动读取 Codex/测评程序预先运行得到的当前模型输出、实际 System/User Prompt 和上下文记录，并与人工核对依据并排展示。文献筛选逐篇选择处理方式；文献阅读逐项核对结论、证据和边界；辅助写作逐项核对引用、修改范围和可用性。完成后点击「保存审核结果」，不需要从 ThesisFlow 复制粘贴。

一次协作循环固定为：

| 顺序 | 你在工作台中做什么 | Codex 随后做什么 |
|---|---|---|
| 1. 查看现状 | 选择一项能力，了解产品位置、当前规则和已有结果 | 维护页面数据，确保 Prompt、规则与实际代码一致 |
| 2. 查看输出 | 直接阅读工作台自动载入的模型输出、实际 Prompt 和人工核对依据 | 预先运行待检查项目，并让每条输出关联唯一任务编号与调用编号 |
| 3. 作出判断 | 对照论文点击「符合 / 不符合 / 无法判断」，选择总体结论 | 不代替你作学术正确性判断 |
| 4. 写下反馈 | 说明事实错误、正确结论和优先修改方向，保存到本地 | 自动把当前模型输出和调用编号一并保存，读取 `evaluation-workbench/data/feedback/` 中的最新反馈 |
| 5. 产品迭代 | 离开工作台，回到 Codex 说明「读取工作台反馈并进行迭代」 | 判断问题发生在代码、检索、上下文、Prompt 或模型，实施修改并完成回归检查 |
| 6. 再次确认 | 在工作台查看新结果并重新审核 | 更新状态和结果；未经你确认，不把候选版本标记为通过 |

你不需要维护 CSV、导出 Prompt 日志、计算指标、分析调用链、复制粘贴模型输出或直接修改 Prompt。你只负责两件事：**判断当前输出是否正确、写清希望修改的地方**。产品运行、结果关联、诊断、实现和结果整理由 Codex 完成。

工作台本地地址为 `http://127.0.0.1:8010`，其代码和数据位于 `evaluation-workbench/`，与 ThesisFlow 的前后端进程分离。

### 阶段 A：准备与标注

1. 冻结研究问题、筛选标准和 24 篇文献的文件版本。
2. 检查 24 篇文献的解析状态，不可用样本只能标记为不可用，不可静默替换。
3. 完成文献清单、30 张任务卡、标准答案、原文依据和评分规则。
4. 分配 dev / holdout，检查同题改写和主张—证据泄漏。
5. 建立 Prompt registry，为每个模型调用入口绑定 Prompt ID、版本、源文件、渲染字段与哈希。
6. 试评 3 题（每场景 1 题），只检查任务卡、Prompt 日志与评分规则是否可用，不用结果调整产品。

### 阶段 B：建立 P0 基线

1. 记录当前 Git 提交、模型、参数、Prompt 版本、数据库快照和运行环境。
2. 恢复统一的起始数据，清理会影响输出的结果缓存，但保留一份原始快照。
3. 按固定顺序执行 21 个 dev 任务和 8 项记忆/反馈回归，完整记录输入与输出。
4. 每题完成后立即评分，先按预定义规则打分，再记录解释。
5. 归类首次失败环节，形成 P0 基线报告。
6. 封存可复现的 P0 版本、数据快照和运行配置，不在此阶段查看或运行 holdout。

### 阶段 C：集中修复与开发集复测

1. 按影响人数、严重度和首次失败环节排序。
2. 每轮只集中修复一类主要问题，例如「检索未命中方法限制」，不同时改多个无法归因的变量。
3. 先复测直接失败题，再执行该环节的完整 dev 回归集。
4. 比较修改前后的逐题结果，不只比较总平均分。
5. 若问题转移到其他环节，记录新的首次失败环节，不将「错误换了位置」记为修复。

### 阶段 D：方案冻结与保留集复测

1. 开发集达到第 10 节门槛后，冻结代码、Prompt、配置和数据处理规则。
2. P0 封存版本与候选版本分别在 9 个 holdout 任务上运行一次；在两版输出都封存前不查看标准答案。技术失败可按预定义重试规则处理。
3. 隐去产品版本标识，将两版输出按随机顺序统一评分，评分完成后再解盲。
4. 生成 P0 vs. 候选版本的逐题对照、指标汇总、失败分布和局限说明。
5. holdout 不达标时，结论为「当前修改未稳定泛化」；返回 dev 时必须建立新版本，不继续称原 holdout 为未见集。

### 阶段 E：本人自用验证

1. 使用冻结版本完成一次真实的文献筛选、精读记录和带引用综述写作。
2. 不将测评任务卡拆分后冒充真实使用；保留自然的任务顺序、中断和返工。
3. 记录系统有用、无帮助、造成误导的具体时刻，以及每次原文核验和人工改写。
4. 自用结果与离线分数分开报告，用于解释工作流问题，不提升为独立用户结论。

## 10. 决策门槛

### 10.1 必过安全门槛

以下项目任一失败，候选版本不进入 holdout：

- 编造或不支持的引用被系统显示为「证据支持」。
- 提案定位或写入失败时静默覆盖、丢失或改变用户正文。
- 回退后的正文与目标快照不一致，且无可恢复副本。
- 已删除记忆仍被使用，或项目 A 的背景事实进入项目 B。
- 拒绝表达方式导致文献来源被自动降权。

### 10.2 开发集进入保留集的已冻结门槛

由于样本小，不设一个看似精确的统计显著性线。候选版本同时满足以下条件时进入 holdout：

1. 第 10.1 节必过门槛全部通过。
2. 目标失败类型的成功题数至少增加 2 题，或对应场景标准化得分提高至少 10 个百分点。
3. 其他任一场景的成功题数不得下降 2 题或以上，标准化得分不得下降超过 5 个百分点。
4. 新增 P0/P1 级失败数为 0。
5. 若宣称提效，则代表任务的中位总耗时至少降低 20%，且质量指标不下降；否则只报告时间差，不使用「显著提效」表述。

第 2、3、5 项为已审核通过的管理阈值，从首次正式基线运行开始冻结，不得根据已看到的结果追溯调整。

### 10.3 holdout 结论规则

- 通过：必过门槛全部通过，三场景中至少两个改善，其余场景无明显回归。
- 有限改善：目标问题改善，但整体样本不足或部分指标不稳定；可保留改动，但明确降低结论强度。
- 不通过：任一必过门槛失败，或改善未在 holdout 复现，或主要回归大于收益。

## 11. 可复现性与运行控制

### 11.1 每次运行必记字段

| 类别 | 字段 |
|---|---|
| 版本 | `run_id`、日期时间、Git commit、分支、数据库快照 ID |
| 模型 | 模型名称/版本、temperature、关键参数、Prompt 版本 |
| 检索 | embedding 模型、top-k、重排方式、检索到的 chunk 键 |
| 上下文 | 项目 ID、记忆开关与活跃记忆、选中批注、正文/大纲状态 |
| 缓存 | 命中/未命中、缓存键或指纹，是否按方案清理 |
| 结果 | 原始输出、界面展示、用户操作、异常日志、任务耗时 |
| 评分 | 各维分数、通过/失败、严重度、首次失败环节、判定备注 |

### 11.2 重试规则

- 模型输出质量差不是技术重试理由，首次有效输出必须记入结果。
- 网络中断、服务 5xx、明确超时或前端未收到响应可重试 1 次，但首次技术失败仍记录为功能可靠性事件。
- 如需第 2 次以上重试，该题在本轮标为未完成，后续运行只用于诊断。
- 手工改 Prompt、换模型、更改文献或修改标准答案后的结果必须使用新 `run_id`，不得覆盖原运行。

### 11.3 时间测量规则

- 所有对照使用相同硬件、网络条件和计时工具。
- 同一类任务使用一致的起止定义；人为中断单独记录，不直接删除。
- 模型等待、人工阅读、原文核验和返工时间分开记录。
- 为降低熟悉度偏差，对照任务采用平衡顺序；无法抵消的学习效应在报告中说明。

## 12. 交付物与文件结构

建议在方案审核通过后建立以下目录：

```text
evaluation/p1/
├── README.md                    # 执行入口与当前状态
├── protocol.md                  # 冻结后的本方案
├── document_manifest.csv        # 文献清单与集合划分
├── prompt_registry.csv           # 各环节 Prompt ID、版本、文件与哈希
├── tasks/
│   ├── screening.csv            # S01–S10
│   ├── reading.csv              # R01–R10
│   └── writing.csv              # W01–W10
├── gold/
│   ├── screening_gold.csv
│   ├── reading_gold.csv
│   └── writing_gold.csv
├── runs/<run_id>/
│   ├── manifest.json             # 版本、模型、参数、环境
│   ├── prompt_calls.jsonl        # System/User Prompt、messages 与上下文清单
│   ├── raw_outputs/              # 原始输出
│   ├── scores.csv                # 逐题评分
│   ├── events.csv                # 耗时、操作、重试
│   └── failures.csv              # 首次失败环节与严重度
└── reports/
    ├── baseline_report.md
    ├── comparison_report.md
    ├── self_use_log.md
    └── iteration_retrospective.md
```

本阶段的正式交付包含：

1. 冻结的产品测评方案。
2. 24 篇文献的清单、分层和 dev / holdout 分配。
3. 30 张任务卡、标准答案、原文依据和评分规则。
4. P0 基线结果、记忆/反馈回归结果和本人自用记录。
5. 每轮候选版本的修复记录、dev 前后对照与回归结果。
6. 最终 holdout 复测报告与简短迭代复盘。

## 13. 报告模板

### 13.1 总结论表

| 场景 | 版本 | 成功数/分母 | 标准化得分 | P0/P1 失败 | 中位耗时 | 结论 |
|---|---|---:|---:|---:|---:|---|
| 筛选 | P0 基线 | 待填 | 待填 | 待填 | 待填 | 待填 |
| 筛选 | 候选版本 | 待填 | 待填 | 待填 | 待填 | 待填 |
| 精读 | P0 基线 | 待填 | 待填 | 待填 | 待填 | 待填 |
| 精读 | 候选版本 | 待填 | 待填 | 待填 | 待填 | 待填 |
| 协作写作 | P0 基线 | 待填 | 待填 | 待填 | 待填 | 待填 |
| 协作写作 | 候选版本 | 待填 | 待填 | 待填 | 待填 | 待填 |

### 13.2 逐题结果必备字段

`task_id`、`split`、`run_id`、`prompt_call_ids`、System/User Prompt 版本、预期结果、实际输出、各维分数、通过/失败、严重度、首次失败环节、下游影响、耗时、重试次数、人工纠正、原文核验结果、备注。

### 13.3 迭代复盘最小结构

1. 发现了什么问题，在多少题中出现。
2. 首次失败环节是什么，证据是什么。
3. 本轮只改了什么，哪些条件保持不变。
4. 原失败题、场景回归集和其他场景分别如何变化。
5. 改动是保留、继续调整还是撤回，依据是什么。
6. 当前结论的适用范围和局限是什么。

## 14. 风险与偏差控制

| 风险 | 影响 | 控制方式 |
|---|---|---|
| 标注者与使用者为同一人 | 容易产生预期偏差 | 先固定依据与规则，保留争议项，不伪装多人一致性 |
| 对文献逐渐熟悉 | 后测耗时自然缩短 | 平衡对照顺序，优先比较质量，单独说明学习效应 |
| 模型随机性 | 同版本结果波动 | 固定参数；开发集对关键任务可做 3 次稳定性检查，holdout 仍按首次有效运行计分 |
| 缓存干扰 | 旧输出被当作新版本结果 | 记录缓存指纹，版本比较时使用预定义的缓存清理规则 |
| 评测集泄漏 | 过度拟合任务卡 | holdout 不进入 Prompt/few-shot，发生泄漏后废弃该保留集并重建 |
| 只看总分 | 严重错误被其他高分抵消 | 同时报告 P0/P1 失败、逐题结果和首次失败环节 |
| 技术运行成本不稳定 | 对照不公平 | 记录调用次数、模型、延迟与失败，相同条件下重跑技术失败项 |

## 15. 测评完成后的迭代调优

### 15.1 总原则：先修复首次失败点

调优方式不由「当前最流行的技术」决定，而由失败证据决定。一个错误如果最早发生在解析或检索，修改 Prompt 可能只会让模型更流畅地使用错误上下文；如果正确证据已进入 Prompt，但模型仍稳定误读，再考虑 Prompt Engineering、模型路由或微调。

每轮使用以下固定闭环：

1. 从基线结果中选择影响最大的一类失败。
2. 沿 Prompt 日志和上下文清单找到首次失败点。
3. 只选择一个主要调优杠杆，写明假设、预期改善和可能回归。
4. 在直接失败题上复测，再跑对应场景的完整 dev 回归集。
5. 达到门槛后冻结候选版，使用 holdout 验证；否则继续调整或撤回。

### 15.2 失败类型与调优手段

| 证据表明的首次失败点 | 优先调优手段 | 不应先做的事 |
|---|---|---|
| 原文未解析、标题/章节/页码错误 | 文件解析、标题识别、OCR/格式兼容和元数据规则 | Prompt Engineering 或模型微调 |
| chunk 截断完整语义，父子块或页码绑定错误 | chunking、Parent-Child 装配和定位元数据 | 用更强模型掩盖上游数据错误 |
| 正确证据不在候选集或 top-k 中 | **RAG 规则调整**：查询改写、稀疏/稠密召回、RRF 权重、top-k、reranker、文献/章节过滤 | 直接改写生成 Prompt |
| 证据已召回，但错误批注或低价值片段排序更高 | **RAG 重排/权重调整**：批注标签权重、tier、文献质量先验、反证分离、去重 | 模型微调 |
| 正确 chunk 已召回，但未被装入实际 Prompt | **上下文装配调整**：长度预算、优先级、截断顺序、去重、正文窗口与批注选入 | 修改检索模型或生成模型 |
| 错项目记忆或过期偏好进入 System Prompt | 记忆范围、时效、冲突规则和当前指令优先级 | 让模型在 Prompt 末尾「自行忽略」错误记忆 |
| 正确证据和记忆均已进入 Prompt，但任务边界或输出格式被误解 | **Prompt Engineering**：重写指令、分层约束、输出 schema、正反例、少量 few-shot、拆分复杂任务 | 一次同时改 RAG、Prompt 和模型 |
| Prompt 清晰、证据完整，但复杂比较、否定或多步推理仍稳定失败 | 先尝试更强模型或按任务路由模型；再评估微调 | 在 30 个测评题上直接微调 |
| 主张与证据对应正确，但校验状态错误 | NLI Prompt/模型、向量快筛阈值、高风险规则和三层状态逻辑 | 改写主生成 Prompt |
| 输出内容正确，但采纳、拒绝、锚点、过期或回退失败 | 确定性代码、状态机、交互反馈和快照逻辑 | Prompt Engineering 或微调 |
| 标准答案、任务或评分规则含糊 | 修正测评资产，重新冻结并记录版本 | 根据模型输出追溯改标准以让它通过 |

### 15.3 Prompt Engineering 的使用条件

当日志证明「正确证据已进入实际 Prompt，但模型未按任务边界使用」时，优先做 Prompt Engineering。调整顺序为：

1. 消除 System Prompt 与 User Prompt 之间的冲突和重复。
2. 把「推荐什么、不能做什么、证据不足怎么做」改为可判定的约束。
3. 使用稳定的 JSON/schema 和枚举值，将格式失败与内容失败分开。
4. 只在已标注的 dev 样例中选择少量具代表性正反例，不引入 holdout 内容。
5. 对复杂任务考虑拆分为「检索/判断 → 生成 → 校验」，并用 `parent_call_id` 连接。
6. 每次只变更一类 Prompt 要素，升级版本号并保存完整前后 diff。

### 15.4 RAG 规则调整的使用条件

当标准证据未进入召回集、排序过低或在装配时被截断，优先调整 RAG。将 RAG 评测与生成评测分开，至少记录：

- 候选召回的 Recall@k、标准证据首次出现的 rank、重排后 rank 和最终装入 Prompt 的证据数。
- 稀疏召回、稠密召回、RRF、reranker、批注提权、文献评分权重和去重各自的中间结果。
- 错误是「未召回」、「已召回但未重排上来」还是「已排上来但被上下文预算裁剪」。

优先调整查询和排序，再扩大 top-k。盲目增大 top-k 会同时增加噪声、token 成本和引用错配风险。

### 15.5 模型替换与微调的使用条件

#### 模型替换/路由

当正确上下文已进入 Prompt、指令也已清晰，但当前模型在跨文献综合、否定关系、证据强度或严格 JSON 输出上仍反复失败，先比较更强模型或按难度路由。除质量外，同时报告延迟、token 和估算成本。

#### 模型微调

本轮 30 个核心任务是评测集，**不足以同时作为微调训练集，也不得直接用于训练**。只在以下条件同时成立时启动微调评估：

1. 剩余错误是稳定、重复的任务模式，而非解析、检索、上下文装配或交互错误。
2. Prompt Engineering 和更强基础模型对照已证明无法在可接受成本内解决。
3. 已从评测集之外积累足够的高质量训练样本，覆盖正例、反例、边界和证据不足情形；数量以学习曲线与独立验证结果决定，不以任意门槛代替数据质量。
4. 训练集、微调验证集与本方案 holdout 完全隔离，不包含同题改写或同一主张—证据单元。
5. 微调的收益能抵消数据制作、训练、模型维护、更新和回归测试成本。

如进入微调，优先针对稳定格式、标签分类或任务边界等清晰目标；引用真实性、文献更新和项目事实仍应依赖检索与确定性校验，不应试图通过微调「记住」。

### 15.6 本项目的建议调优优先级

根据 ThesisFlow 当前架构和本轮小样本设计，建议顺序为：

1. **确定性功能和数据链路**：先修复解析、定位、引用状态、采纳/回退和记忆隔离问题。
2. **RAG 与上下文装配**：确保正确证据能被召回、排到前列并真正进入 Prompt。
3. **Prompt Engineering**：在证据链路正确后，处理任务边界、不确定性表达、标签语义、输出格式和审查标准。
4. **模型路由/替换**：只对需要复杂综合、NLI 或高风险判定的任务升级模型，不全链路无差别增加成本。
5. **微调**：不作为当前 P1 默认方案；只在积累了额外高质量训练数据并证明有稳定残余错误后立项。

## 16. 执行启动清单

### 16.1 当前执行状态（2026-09-13）

- [x] 已创建 `evaluation/p1/` 测评目录、24 篇文献清单、30 张任务卡、评分表、Prompt registry、运行目录与自用日志模板。
- [x] 已实现 System/User Prompt、上下文清单、模型参数、原始输出、解析结果、异常信息的全链路留痕与 JSONL 导出，并完成静态检查、持久化检查和接口冒烟测试。
- [x] 已预填 S01、R01、W01 三张试评任务卡，状态统一标记为「待人工确认」，未将模型输出当作标准答案。
- [ ] **人工关口：**你需要核对三张试评卡的原文依据、预期结论、可接受答案与严重错误条件；确认后方可冻结试评标准。
- [ ] 完成其余任务的人工标注、冲突处理和版本冻结后，再启动 P0 基线运行；当前尚未使用试评结果调优产品。

### 16.2 启动步骤

1. 由 Codex 在 `evaluation/p1/` 下创建文献清单、任务卡、评分表、Prompt registry 和运行目录模板。
2. 在产品中补齐第 4.3 节规定的 System/User Prompt 全链路记录、脱敏与导出能力，并通过完整性检查。
3. 由 Codex 预填 S01、R01、W01 三张试评任务卡与原文依据，由你完成学术判断与最终确认。
4. 试评只用来校验字段、日志和量表，不根据试评输出调优产品。
5. 完成全部人工标注和集合划分后，冻结测评集并开始 P0 基线运行。
